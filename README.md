
# 🚀 Bazary - AI-Powered Telegram Merchant Studio

Bazary — это инновационная платформа для быстрого запуска продаж в Telegram. Мы превращаем обычные фотографии товаров в профессиональный контент для магазинов с помощью магии Google Gemini AI.

## ⚙️ Настройка окружения (Windows)

Если у вас не установлен Git, выполните эту команду в PowerShell от имени администратора:
```powershell
winget install --id Git.Git -e --source winget
```
*После установки перезапустите терминал.*

## 📦 Как отправить код в GitHub (Git-flow)

1. **Инициализация**: `git init`
2. **Добавить файлы**: `git add .`
3. **Зафиксировать**: `git commit -m "Initial commit"`
4. **Ветка**: `git branch -M main`
5. **Связь с сервером**: `git remote add origin https://github.com/ВАШ_ЛОГИН/bazary-ai-studio.git`
6. **Отправить**: `git push -u origin main`

## 🛠 Решение ошибок доступа и системных функций

### 1. Ошибка "Permission Denied"
Выполните в PowerShell (Admin):
```powershell
takeown /f . /r /d y
icacls . /grant Everyone:F /t /c /q
git config --global --add safe.directory "*"
```

### 2. Ошибка "Function not implemented"
Обычно возникает в папках OneDrive или на сетевых дисках. Выполните:
```powershell
# Отключить проверку режимов файлов и симлинки
git config --global core.filemode false
git config --global core.symlinks false

# Удалить поврежденный индекс (если есть)
Remove-Item -Path .git/index -ErrorAction SilentlyContinue

# Попробовать добавить снова
git add --all
```

> **Совет:** Если команды выше не помогают, перенесите проект из папки "Документы" (которая синхронизируется с OneDrive) в корень диска `C:\bazary`.

---
Разработано с любовью к e-commerce и искусственному интеллекту.
